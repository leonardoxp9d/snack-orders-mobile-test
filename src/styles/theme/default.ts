import { DefaultTheme } from 'styled-components';

const defaultTheme: DefaultTheme = {
  title: 'default',

  colors: {
    background: '#312e38', // cor do backgraund, cor da letra do botão
    primaryColor: '#FF9000',
    white: '#f4ede8', // letra
    gray: '#999591', // letra
    grayHard: '#666360', // svg, letra input
    inputs: '#232129',
    shape: '#3E3B47', // shape para demostração tipo input, no background
    blackMedium: '#28262E', // para da um contraste entre o background
    error: '#c53030',
  },
};

export default defaultTheme;
